# Development JSON Files

Development JSON files live under `data/`.

Known files:

- `data/projects.json`
- `data/prompt-library-presets.json`

These are written through Vite middleware. They are local development conveniences, not production database tables.
