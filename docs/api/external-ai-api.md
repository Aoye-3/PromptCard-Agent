# External AI API

The Agent Runtime is configured for DeepSeek-compatible chat completion through `agent-runtime/config.yaml`.

The frontend also contains legacy AI service configuration for external API access through the Vite `/api` proxy.

Do not document or commit real API keys. Local key discovery is described in [Secrets Policy](../operations/secrets-policy.md).
